@model eShop.Models.MaterialSlipProductionWorkOrder

@{
    Layout = null;
}

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <h4 class="modal-title">Edit - Detail </h4>
        </div>
        <div class="modal-body">
            @Html.Partial("MvcDatalist/_Dialog")
            <div id="divEmp">
                @using (Ajax.BeginForm("WorkOrdersEdit", "MaterialSlips", new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "divEmp", OnSuccess = "EditSuccess" }))
                {
                    @Html.AntiForgeryToken()

                <div class="form-horizontal">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    @Html.HiddenFor(model => model.Id)
                    @Html.HiddenFor(model => model.MaterialSlipId)
                    <div class="form-group">
                        <div class="col-md-12">
                            @Html.LabelFor(model => model.ProductionWorkOrderId)
                            @Html.DatalistFor(model => model.ProductionWorkOrderId, new eShop.Models.OutstandingProductionWorkOrderDatalist(), new { @class = "datalist2" })
                            @Html.ValidationMessageFor(model => model.ProductionWorkOrderId, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-12 text-right">
                            <input type="submit" value="Save" class="btn btn-primary" style="margin-top:10px" />
                        </div>
                    </div>
                </div>
                }
            </div>
        </div>
    </div>
</div>