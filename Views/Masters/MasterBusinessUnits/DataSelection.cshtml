@model eShop.Models.MasterBusinessUnit

@{
    ViewBag.Title = "Data Selection";
    ViewBag.Current = "MasterBusinessUnits";
}

@section Styles {
    @Styles.Render("~/style/mvcgrid")
    @Styles.Render("~/style/MvcDatalist")
}

<style>
    .tablegrid-hover tbody tr:hover {
        background-color: rgba(0, 0, 0, 0.1);
    }
</style>

@Html.Partial("MvcDatalist/_Dialog")

<div class="content-wrapper">
    <section class="content-header-fixed">
        <h1 class="col-md-6">
            @Model.Name
        </h1>
    </section>

    <!-- Main content -->
    <section class="content" style="margin-top:30px;">
        <div class="row">
            <div class="col-md-12">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <i class="fa fa-database"></i>

                        <h3 class="box-title">Data Master</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            <div class="form-group">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterRegions" data-dismiss="modal">
                                                Wilayah
                                            </button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterSuppliers" data-dismiss="modal">
                                                Supplier
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterCustomers" data-dismiss="modal">
                                                Konsumen
                                            </button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnSalesPersons" data-dismiss="modal">
                                                Sales
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterWarehouses" data-dismiss="modal">
                                                Gudang
                                            </button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterAccounts" data-dismiss="modal">
                                                Akun
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>

            <!-- /.col -->
            <div class="col-md-12">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <i class="fa fa-cube"></i>

                        <h3 class="box-title">Inventory</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            <div class="form-group">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterCategories" data-dismiss="modal">
                                                Kategori
                                            </button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterBrands" data-dismiss="modal">
                                                Merek
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="col-md-12">
                                            <button type="button" class="btn btn-primary btn-block btn-lg btnMasterItems" data-dismiss="modal">
                                                Barang
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>

            <div class="col-md-12">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <i class="fa fa-database"></i>

                        <h3 class="box-title">Setting</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            <div class="form-group">
                                <div class="col-md-6">
                                    <button type="button" class="btn btn-primary btn-block btn-lg btnAccountsSetting" data-dismiss="modal">
                                        Setting Akun
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!-- /.col -->
        </div>
    </section>
</div>
<div class="modal fade" id="detailsModal" tabindex="-2" role="dialog" aria-hidden="true" data-backdrop="static">
    <div id="detailsContainer">
    </div>
</div>

<div class="modal fade" id="subdetailsModal" tabindex="-1" role="dialog" aria-hidden="true" data-backdrop="static">
    <div id="subdetailsContainer">
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/mvcgrid")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/unobtrusive")
    @Scripts.Render("~/script/tableHeadFixer")
    @Scripts.Render("~/bundles/MvcDatalist")
    @Scripts.Render("~/script/InputMask")

    <script>
        $.validator.methods.range = function (value, element, param) {
            var globalizedValue = value.replace(",", ".");
            return this.optional(element) || (globalizedValue >= param[0] && globalizedValue <= param[1]);
        }

        $.validator.methods.number = function (value, element) {
            return this.optional(element) || /-?(?:\d+|\d{1,3}(?:[\s\.,]\d{3})+)(?:[\.,]\d+)?$/.test(value);
        }
    </script>

    <script>
        [].forEach.call(document.getElementsByClassName('mvc-grid'), function (element) {
            new MvcGrid(element);
        });

        $('#btnAdd').click(function (event) {
            event.preventDefault();
            var url = $(this).attr("href");
            $.get(url, function (data) {
                $('#detailsContainer').html(data);
                $('#detailsModal').modal('show');

                $('form').each(function () {
                    var $el = $(this);
                    $el.data('validator', null);
                    $.validator.unobtrusive.parse($el);
                });
            });
        });

        function CreateSuccess(data) {
            if (data !== "success") {
                $('#detailsContainer').html(data);
                return;
            }
            $('#detailsModal').modal('hide');
            $('#detailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#data-table'));
            grid.reload();
        }

        function EditSuccess(data) {
            if (data !== "success") {
                $('#detailsContainer').html(data);
                return;
            }
            $('#detailsModal').modal('hide');
            $('#detailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#data-table'));
            grid.reload();
        }

        function CostsCreateSuccess(data) {
            if (data !== "success") {
                $('#subdetailsContainer').html(data);
                return;
            }
            $('#subdetailsModal').modal('hide');
            $('#subdetailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#costs-table'));
            grid.reload();
        }

        function CostsEditSuccess(data) {
            if (data !== "success") {
                $('#subdetailsContainer').html(data);
                return;
            }
            $('#subdetailsModal').modal('hide');
            $('#subdetailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#costs-table'));
            grid.reload();
        }

        function AccountsCreateSuccess(data) {
            if (data !== "success") {
                $('#subdetailsContainer').html(data);
                return;
            }
            $('#subdetailsModal').modal('hide');
            $('#subdetailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#accounts-table'));
            grid.reload();
        }

        function AccountsEditSuccess(data) {
            if (data !== "success") {
                $('#subdetailsContainer').html(data);
                return;
            }
            $('#subdetailsModal').modal('hide');
            $('#subdetailsContainer').html("");

            var grid = new MvcGrid(document.querySelector('#accounts-table'));
            grid.reload();
        }
    </script>

    <script>
        var token = $('input[name="__RequestVerificationToken"]').val();
        var headers = {};
        headers['__RequestVerificationToken'] = token;

        $(document).ready(function () {
            $("#batchDelete").click(function () {
                var selectedIDs = new Array();
                $('#data-table tbody tr').find('td:first :checkbox').each(function () {
                    if ($(this).prop('checked')) {
                        selectedIDs.push($(this).val());
                    }
                });

                if (selectedIDs.length <= 0) {
                    bootbox.alert("Pilih salah satu data yang akan dihapus.");
                }
                else {
                    bootbox.confirm("Apakah anda yakin akan menghapus " + selectedIDs.length + " data?", function (result) {
                        if (result) {
                            var options = {};
                            options.url = "/MasterBusinessUnits/BatchDelete";
                            options.type = "POST";
                            options.headers = headers;
                            options.data = JSON.stringify(selectedIDs);
                            options.contentType = "application/json";
                            options.dataType = "json";
                            options.success = function (msg) {
                                bootbox.alert(msg);

                                var grid = new MvcGrid(document.querySelector('#data-table'));
                                grid.reload();
                            };
                            options.error = function () {
                                bootbox.alert("Terjadi kesalahan saat menghapus.");
                            };
                            $.ajax(options);
                        }
                    });
                }
            });
        });

        document.addEventListener('reloadend', function (e) {
            if (e.detail.grid.element.id == 'data-table') {
                $('#CheckAll').click(function () {
                    var checkedStatus = this.checked;
                    $('#data-table tbody tr').find('td:first :checkbox').each(function () {
                        $(this).prop('checked', checkedStatus);
                    });
                });

                $('#data-table').on("click", ".edit-data", function (event) {
                    event.preventDefault();
                    var url = $(this).attr("href");
                    $.get(url, function (data) {
                        $('#detailsContainer').html(data);
                        $('#detailsModal').modal('show');

                        $('form').each(function () {
                            var $el = $(this);
                            $el.data('validator', null);
                            $.validator.unobtrusive.parse($el);
                        });

                        [].forEach.call(document.getElementsByClassName('mvc-grid'), function (element) {
                            new MvcGrid(element);
                        });

                        Inputmask("myNum2").mask("#PPNRate");
                        Inputmask("myNum2").mask("#FCLLoadFee");
                        Inputmask("myNum2").mask("#LCLLoadFee");
                        Inputmask("myNum2").mask("#EmptyLoadFee");

                        $('#btnCostsAdd').click(function (event) {
                            event.preventDefault();
                            var url = $(this).attr("href");
                            $.get(url, function (data) {
                                $('#subdetailsContainer').html(data);
                                $('#subdetailsModal').modal('show');

                                $('form').each(function () {
                                    var $el = $(this);
                                    $el.data('validator', null);
                                    $.validator.unobtrusive.parse($el);
                                });

                                [].forEach.call(document.getElementsByClassName('datalist2'), function (element) {
                                    new MvcDatalist(element);
                                });
                            });
                        });

                        $('#btnAccountsAdd').click(function (event) {
                            event.preventDefault();
                            var url = $(this).attr("href");
                            $.get(url, function (data) {
                                $('#subdetailsContainer').html(data);
                                $('#subdetailsModal').modal('show');

                                $('form').each(function () {
                                    var $el = $(this);
                                    $el.data('validator', null);
                                    $.validator.unobtrusive.parse($el);
                                });

                                [].forEach.call(document.getElementsByClassName('datalist2'), function (element) {
                                    new MvcDatalist(element);
                                });
                            });
                        });

                        var token2 = $('input[name="__RequestVerificationToken"]').val();
                        var headers2 = {};
                        headers2['__RequestVerificationToken'] = token2;

                        $("#costsBatchDelete").click(function (event) {
                            event.preventDefault();
                            var selectedIDs = new Array();
                            $('#costs-table tbody tr').find('td:first :checkbox').each(function () {
                                if ($(this).prop('checked')) {
                                    selectedIDs.push($(this).val());
                                }
                            });

                            if (selectedIDs.length <= 0) {
                                bootbox.alert("Pilih salah satu data yang akan dihapus.");
                            }
                            else {
                                bootbox.confirm("Apakah anda yakin akan menghapus " + selectedIDs.length + " data?", function (result) {
                                    if (result) {
                                        var options = {};
                                        options.url = "/MasterBusinessUnits/CostsBatchDelete";
                                        options.type = "POST";
                                        options.headers = headers2;
                                        options.data = JSON.stringify(selectedIDs);
                                        options.contentType = "application/json";
                                        options.dataType = "json";
                                        options.success = function (msg) {
                                            bootbox.alert(msg);

                                            var grid = new MvcGrid(document.querySelector('#costs-table'));
                                            grid.reload();
                                        };
                                        options.error = function () {
                                            bootbox.alert("Terjadi kesalahan saat menghapus.");
                                        };
                                        $.ajax(options);
                                    }
                                });
                            }
                        });

                        $("#accountsBatchDelete").click(function (event) {
                            event.preventDefault();
                            var selectedIDs = new Array();
                            $('#accounts-table tbody tr').find('td:first :checkbox').each(function () {
                                if ($(this).prop('checked')) {
                                    selectedIDs.push($(this).val());
                                }
                            });

                            if (selectedIDs.length <= 0) {
                                bootbox.alert("Pilih salah satu data yang akan dihapus.");
                            }
                            else {
                                bootbox.confirm("Apakah anda yakin akan menghapus " + selectedIDs.length + " data?", function (result) {
                                    if (result) {
                                        var options = {};
                                        options.url = "/MasterBusinessUnits/AccountsBatchDelete";
                                        options.type = "POST";
                                        options.headers = headers2;
                                        options.data = JSON.stringify(selectedIDs);
                                        options.contentType = "application/json";
                                        options.dataType = "json";
                                        options.success = function (msg) {
                                            bootbox.alert(msg);

                                            var grid = new MvcGrid(document.querySelector('#accounts-table'));
                                            grid.reload();
                                        };
                                        options.error = function () {
                                            bootbox.alert("Terjadi kesalahan saat menghapus.");
                                        };
                                        $.ajax(options);
                                    }
                                });
                            }
                        });
                    });
                });

                $('#data-table').on("click", ".view-data", function (event) {
                    event.preventDefault();
                    var url = $(this).attr("href");
                    $.get(url, function (data) {
                        $('#detailsContainer').html(data);
                        $('#detailsModal').modal('show');
                    });
                });

                $('.fix-table').tableHeadFixer({ left: 3 });
            }
            else if (e.detail.grid.element.id == 'accounts-table') {
                $('#accounts-table').on("click", ".edit-account", function (event) {
                    event.preventDefault();
                    var url = $(this).attr("href");
                    $.get(url, function (data) {
                        $('#subdetailsContainer').html(data);
                        $('#subdetailsModal').modal('show');

                        [].forEach.call(document.getElementsByClassName('datalist2'), function (element) {
                            new MvcDatalist(element);
                        });
                    });
                });
            }
            else if (e.detail.grid.element.id == 'costs-table') {
                $('#costs-table').on("click", ".edit-cost", function (event) {
                    event.preventDefault();
                    var url = $(this).attr("href");
                    $.get(url, function (data) {
                        $('#subdetailsContainer').html(data);
                        $('#subdetailsModal').modal('show');

                        [].forEach.call(document.getElementsByClassName('datalist2'), function (element) {
                            new MvcDatalist(element);
                        });
                    });
                });
            }
        });
    </script>
}
